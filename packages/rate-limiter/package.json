{
  "name": "@bocam/rate-limiter",
  "version": "1.0.0",
  "description": "Rate limiting de aplicación (Redis-backed, con fallback a memoria) compartido por todos los microservicios de negocio del ecosistema BOCAM",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test --require ts-node/register src/**/*.test.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "rate-limit-redis": "^4.3.1",
    "redis": "^4.7.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.1.3"
  },
  "author": "Constructora Bocam, S. A. de C.V.",
  "license": "UNLICENSED"
}
