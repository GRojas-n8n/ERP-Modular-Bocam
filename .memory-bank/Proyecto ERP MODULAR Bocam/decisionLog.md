# Decision Log

## 2026-03-20

- Se fijo el MCP de Memory Bank a una dependencia local del repositorio para evitar depender de `npx @latest`
- Se mantuvo `MEMORY_BANK_ROOT` dentro del workspace para que el banco viaje con el proyecto
- Se habilitaron operaciones base en `autoApprove` para uso fluido desde el cliente MCP
